{
  "name": "sortfree-extension",
  "version": "2.0.0",
  "description": "Sort and organize any web content using natural language prompts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "typescript": "^5.6.3"
  }
} 